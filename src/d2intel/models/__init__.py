"""Слой моделей: регистрация версии, baseline и запись предсказаний.

Владение: это НЕ делегированная часть (FEAT-001 у второго агента — в `src/features/`).
Схема predсказаний уже существует в миграции 0001 (`model_version`,
`prediction`, `prediction_snapshot`, `prediction_evaluation`) — здесь появляется
код, который в неё пишет.

`ML-001` требует prior + LR baseline; первым идёт **prior** — константный
прогноз, обученный только на train. Это не «модель ради модели»: это
обязательный нижний ориентир из ADR-006, и заодно он проверяет всю цепочку
записи предсказаний end-to-end до того, как появятся признаки.
"""
